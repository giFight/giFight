import React from 'react';

//* CURRENTLY NOT USING THIS COMPONENT, MAY COME BACK TO IT

const NoGifs = props => (
  <li className='no-gifs' style={{textAlign: 'center', listStyle: 'none'}}>
    <h3>Sorry, no GIFs match your search.</h3>
  </li>
);

export default NoGifs;